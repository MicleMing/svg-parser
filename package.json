{
  "name": "m-svg-parser",
  "version": "1.0.0",
  "description": "parse svg to virtual dom",
  "main": "dist/index.js",
  "scripts": {
    "build": "rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "svg",
    "virtual-dom"
  ],
  "author": "lanmingming",
  "license": "MIT",
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "rollup": "^0.56.4"
  }
}
